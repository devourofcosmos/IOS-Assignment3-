import SwiftUI

struct AccountView: View {
    var body: some View {
        Text("Account View")
            .font(.largeTitle)
            .padding()
    }
}

struct AccountView_Previews: PreviewProvider {
    static var previews: some View {
        AccountView()
    }
}
