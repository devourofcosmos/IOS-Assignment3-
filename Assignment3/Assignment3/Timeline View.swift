import SwiftUI

struct TimelineView: View {
    var body: some View {
        Text("Timeline View")
            .font(.largeTitle)
            .padding()
    }
}

struct TimelineView_Previews: PreviewProvider {
    static var previews: some View {
        TimelineView()
    }
}
